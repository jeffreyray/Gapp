package Gapp::Table::Util;

use Moose;
use Sub::Exporter;

Sub::Exporter::setup_exporter({
    #exports => [qw( resolve_widget_trait_alias )],
    #groups  => { all => [qw( resolve_widget_trait_alias )] }
});

1;